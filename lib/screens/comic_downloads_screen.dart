import 'package:flutter/material.dart';

class ComicDownloadsScreen extends StatelessWidget {
  const ComicDownloadsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("还没开发完鸭"),
      ),
    );
  }
}
